---
import Icon from './Icon.astro';
---

<section id="informacion" class="py-20 px-4 sm:px-6 lg:px-8 bg-gradient-to-br from-blue-50 via-purple-50 to-pink-50">
  <div class="max-w-6xl mx-auto">
    <!-- Header Section -->
    <div class="text-center mb-12">
      <div class="inline-block mb-4">
        <span class="inline-flex items-center gap-2 bg-green-100 text-green-700 text-sm font-medium px-4 py-1.5 rounded-full">
          <Icon name="location_on" className="text-green-700" size={16} />
          <span>Información del Evento</span>
        </span>
      </div>
      <h2 class="text-4xl sm:text-5xl font-bold text-gray-800 mb-4">
        Detalles del Baby Shower
      </h2>
      <p class="text-lg text-gray-600">
        Toda la información que necesitas para celebrar con nosotros
      </p>
    </div>

    <!-- Two Column Cards -->
    <div class="grid md:grid-cols-2 gap-6 lg:gap-8">
      <!-- Left Card - Event Details -->
      <div class="bg-white rounded-3xl p-8 shadow-lg flex flex-col justify-center">
        <!-- Date -->
        <div class="flex items-center gap-4 mb-6">
          <div class="flex-shrink-0 w-12 h-12 bg-pink-100 rounded-full flex items-center justify-center">
            <Icon name="calendar_today" className="text-pink-700" size={24} />
          </div>
          <div>
            <p class="text-gray-800 font-semibold">Fecha</p>
            <p class="text-gray-600">Domingo, 8 de Febrero 2026</p>
          </div>
        </div>

        <!-- Time -->
        <div class="flex items-center gap-4 mb-6">
          <div class="flex-shrink-0 w-12 h-12 bg-purple-100 rounded-full flex items-center justify-center">
            <Icon name="schedule" className="text-purple-700" size={24} />
          </div>
          <div>
            <p class="text-gray-800 font-semibold">Horario</p>
            <p class="text-gray-600">2:00 PM</p>
          </div>
        </div>

        <!-- Location -->
        <div class="flex items-start gap-4 mb-8">
          <div class="flex-shrink-0 w-12 h-12 bg-blue-100 rounded-full flex items-center justify-center mt-1">
            <Icon name="location_on" className="text-blue-700" size={24} />
          </div>
          <div>
            <p class="text-gray-800 font-semibold">Ubicación</p>
            <p class="text-gray-600">Mosquera, Roble Novaterra</p>
            <p class="text-gray-600">Cll 17 # 7 - 58, Salón Social</p>
          </div>
        </div>

        <!-- Buttons -->
        <div class="space-y-3">
          <button
            onclick="window.open('https://maps.app.goo.gl/K4muBdjfNymy8Vtq9', '_blank')"
            class="w-full bg-blue-500 hover:bg-blue-600 text-white font-semibold py-3 px-6 rounded-xl flex items-center justify-center gap-2 transition-colors shadow-md hover:shadow-lg"
          >
            <Icon name="directions" size={20} />
            <span>Cómo Llegar</span>
          </button>
          <button
            onclick="document.getElementById('confirmar')?.scrollIntoView({ behavior: 'smooth' })"
            class="w-full bg-pink-500 hover:bg-pink-600 text-white font-semibold py-3 px-6 rounded-xl flex items-center justify-center gap-2 transition-colors shadow-md hover:shadow-lg"
          >
            <Icon name="keyboard_arrow_down" size={20} />
            <span>Confirmar mi asistencia <Icon name="arrow_downward" size={20} /></span>
          </button>
        </div>
      </div>

      <!-- Right Card - Welcome & Recommendations -->
      <div class="bg-white rounded-3xl shadow-lg overflow-hidden">
        <!-- Welcome Message -->
        <img 
            src="/banner.png" 
            alt="TE ESPERAMOS ESE DIA MAGICO LLENO DE AMOR - Familia en coche y recomendaciones" 
            class="w-full h-auto object-contain"
          />

        <!-- Recommendations -->
        <div class="p-8">
          <h4 class="text-xl font-bold text-gray-800 mb-4">Recomendaciones</h4>
          <ol class="space-y-3">
            <li class="flex items-start gap-3">
              <span class="flex-shrink-0 w-6 h-6 bg-pink-100 rounded-full text-pink-700 text-sm font-semibold flex items-center justify-center">1</span>
              <span class="text-gray-700">Por favor confirma tu asistencia con anticipación</span>
            </li>
            <li class="flex items-start gap-3">
              <span class="flex-shrink-0 w-6 h-6 bg-purple-100 rounded-full text-purple-700 text-sm font-semibold flex items-center justify-center">2</span>
              <span class="text-gray-700">El estacionamiento está disponible en el lugar</span>
            </li>
            <li class="flex items-start gap-3">
              <span class="flex-shrink-0 w-6 h-6 bg-blue-100 rounded-full text-blue-700 text-sm font-semibold flex items-center justify-center">3</span>
              <span class="text-gray-700">Habrá comida y bebidas para todos</span>
            </li>
            <li class="flex items-start gap-3">
              <span class="flex-shrink-0 w-6 h-6 bg-green-100 rounded-full text-green-700 text-sm font-semibold flex items-center justify-center">4</span>
              <span class="text-gray-700">¡Ven con tu mejor sonrisa y ganas de celebrar!</span>
            </li>
          </ol>
        </div>
      </div>
    </div>
  </div>
</section>
