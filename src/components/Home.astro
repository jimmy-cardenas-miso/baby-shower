---
import familyImage from '../assets/family.png';
import Icon from './Icon.astro';
---

<section id="inicio" class="relative min-h-screen flex items-center bg-gradient-to-br from-pink-50 via-purple-50 to-blue-50 py-16 sm:py-20 px-4 sm:px-6 lg:px-8">
  <div class="max-w-7xl mx-auto w-full">
    <div class="grid lg:grid-cols-2 gap-8 lg:gap-12 items-center">
      <!-- Left Column - Text Content -->
      <div class="space-y-6">
        <!-- Tag -->
        <div class="inline-block">
          <span class="inline-flex items-center gap-2 bg-pink-100 text-pink-700 text-sm font-medium px-4 py-2 rounded-full">
            <Icon name="celebration" className="text-pink-700" size={16} />
            <span>Una celebración especial</span>
          </span>
        </div>

        <!-- Title -->
        <h1 class="text-5xl sm:text-6xl lg:text-7xl font-bold text-gray-900 leading-tight">
          Baby Shower
        </h1>

        <!-- Name -->
        <h2 class="text-5xl sm:text-6xl lg:text-7xl font-bold text-pink-500 leading-tight">
          Emily
        </h2>

        <!-- Paragraphs -->
        <div class="space-y-4 pt-4">
          <p class="text-gray-600 text-lg leading-relaxed">
            Te invitamos a celebrar la llegada de nuestro más grande tesoro.
          </p>
          <p class="text-gray-600 text-lg leading-relaxed">
            Revisa aquí los detalles, confirma tu asistencia, comparte un detalle y un mensaje para disfrutar un momento mágico lleno de amor, risas y bendiciones.
          </p>
        </div>

        <!-- Button -->
        <div class="pt-4">
          <button
            onclick="document.getElementById('informacion')?.scrollIntoView({ behavior: 'smooth' })"
            class="bg-pink-500 hover:bg-pink-600 text-white font-semibold py-4 px-8 rounded-xl flex items-center gap-2 transition-colors shadow-lg hover:shadow-xl"
          >
            <span>¡Me encanta! Quiero saber más</span>
            <Icon name="keyboard_arrow_down" size={20} />
          </button>
        </div>
      </div>

      <!-- Right Column - Illustration Card -->
      <div class="relative">
        <div class="bg-white rounded-3xl p-8 sm:p-10 shadow-lg">
          <!-- Family Illustration -->
          <div class="relative w-full max-w-full aspect-square mx-auto bg-gradient-to-br from-pink-100 via-purple-100 to-blue-100 rounded-2xl p-4 sm:p-6">
            <img 
              src={familyImage.src}
              alt="Familia feliz con bebé" 
              class="w-full h-full object-contain"
              width={familyImage.width}
              height={familyImage.height}
            />
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
